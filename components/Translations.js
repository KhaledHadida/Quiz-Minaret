import English from '../quizzes/English/General Text.json'
import Arabic from '../quizzes/Arabic/General Text.json'

//Created a separate class in case it scales a lot (i.e if there happens to be need of adding more languages)
const Translations = {
    English,
    Arabic,
};

export default Translations;