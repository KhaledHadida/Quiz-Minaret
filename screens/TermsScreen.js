import { SafeAreaView } from "react-native-safe-area-context";
import { Text } from "react-native";

//Was supposed to create a Terms & Privacy, however I changed my mind on doing this since I may not publish this on PlayStore
//Instead I'll most likely put it on my site (portfolio) for those interested.
const TermsScreen = () => {
    
return(
<SafeAreaView>
    <Text>
        This was originally the placeholder for the terms & privacy, however due to change in plans of not publishing the app, I've left it undone for now.
    </Text>
</SafeAreaView>
);
}

export default TermsScreen;
